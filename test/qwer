#!/usr/bin/env python

from qwer import *
from testify import *

a = qwer('Expect')
b = qwer(rule('a'))

# Unbound
equal('(Expect)', b.a)
equal('(Expect)', b['a'])
equal('Expect', b.bogus)

# Bound
equal('Expect', str(b('ExpectRest').a))
equal('Expect', str(b('ExpectRest')['a']))
